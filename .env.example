# 仅用于独立调试 Agent(本地直接 node dist/server.js)。
# 正常通过 Spring Boot 调用时,key/model 由请求 body 注入,无需本文件。

PORT=3000

# 仅本地调试使用——生产环境绝不写入镜像或配置文件。
# OPENROUTER_API_KEY=sk-or-v1-xxxxxxxx
# OPENROUTER_MODEL=anthropic/claude-sonnet-4.5
